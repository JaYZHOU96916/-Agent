[project]
name = "data-analysis-agent"
version = "0.1.0"
description = "Production-grade code-interpreter data analysis agent"
requires-python = ">=3.14"

[tool.pytest.ini_options]
testpaths = ["backend/tests"]
asyncio_default_fixture_loop_scope = "function"
